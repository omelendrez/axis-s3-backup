{
  "name": "axis-s3-backup",
  "version": "1.0.0",
  "description": "Scheduled service to backup axis-documents files to AWS S3",
  "main": "app/index.js",
  "engines": {
    "node": "16.20.2"
  },
  "scripts": {
    "dev": "nodemon ./app",
    "start": "node ./app",
    "lint": "eslint ./app --ext .js",
    "lint:fix": "eslint --fix ./app --ext .js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.x",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "nodemon": "^3.0.2"
  }
}
